# Launch Prototype — Archived 2026-04-08

This is an earlier experimental landing page from the original rebrand (commit `532af60` — "feat: rebrand README and add interactive launch page"). It was never promoted to production and was drifting out of sync with the canonical landing page at `docs/index.html`.

**Status:** Archived. Not served by GitHub Pages. Not linked from anywhere in the repo.

**Why it was archived instead of deleted:** Some of the design work (layout, interactive elements) may be worth revisiting. `git log` preserves the history either way, but keeping the file readable in-repo makes that reference easier.

**Known stale content at archive time:**

- "Code Context" naming (should be "Claude Context" — fixed in `docs/index.html` via PR #5)
- Upstream attribution URL `github.com/zilliztech/mcp-server-milvus` (should be `github.com/zilliztech/claude-context` — same fix)
- "MIT License" footer (repo is actually GPL-3.0 — fixed in `docs/index.html` via PR #6)

**Do not edit this file or its siblings.** If you want to bring parts of the design back, start from scratch in `docs/index.html` and copy what you want from here as a reference — do not promote this file directly.

**Canonical landing page:** `docs/index.html`
